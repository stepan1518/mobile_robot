services:
  flyway:
    build:
      context: .
      dockerfile: Dockerfile.flyway
    env_file:
      - ${ENV_PROPERTIES}
    command: migrate
    extra_hosts:
      - "host.docker.internal:host-gateway"
    profiles: ["migrate"]